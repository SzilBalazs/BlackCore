name: Perft test and benchmark

on:
  push:
    branches: [ "master"]
  pull_request:
    branches: [ "master" ]

jobs:
  BlackCore:
    name: ${{matrix.config.name}}
    runs-on: ${{matrix.config.os}}
    strategy:
      matrix:
        config:
          - {
              name: "Ubuntu g++ popcnt",
              os: ubuntu-latest,
              compiler: g++-11,
              arch: popcnt,
              target: BlackCore,
              upload_target: true,
              artifact_name: "BlackCore-popcnt-linux"
          }
          - {
              name: "Ubuntu g++ bmi2",
              os: ubuntu-latest,
              compiler: g++-11,
              arch: bmi2,
              target: BlackCore,
              upload_target: true,
              artifact_name: "BlackCore-bmi2-linux"
          }
          - {
              name: "Ubuntu clang popcnt",
              os: ubuntu-latest,
              compiler: clang++-11,
              arch: popcnt,
              target: BlackCore,
              upload_target: false,
              artifact_name: ""
          }
          - {
              name: "Ubuntu clang bmi2",
              os: ubuntu-latest,
              compiler: clang++-11,
              arch: bmi2,
              target: BlackCore,
              upload_target: false,
              artifact_name: ""
          }
          - {
              name: "Windows g++ popcnt",
              os: windows-latest,
              compiler: g++,
              arch: popcnt,
              target: BlackCore.exe,
              upload_target: true,
              artifact_name: "BlackCore-popcnt-win"
          }
          - {
              name: "Windows g++ bmi2",
              os: windows-latest,
              compiler: g++,
              arch: bmi2,
              target: BlackCore.exe,
              upload_target: true,
              artifact_name: "BlackCore-bmi2-win"
          }

    steps:
    - uses: actions/checkout@v3
    - name: Installing packages
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install g++-11 clang++-11
    - name: Making BlackCore 
      run: |
        cd src
        make CXX=${{matrix.config.compiler}} EXE=${{matrix.config.target}} ARCH=${{matrix.config.target}}
    - name: Running perft test
      run: | 
        cd src
        ./${{matrix.config.target}} perft
    - name: Running benchmark
      run: | 
        cd src
        ./${{matrix.config.target}} bench
    - name: Upload artifact
      if: ${{matrix.config.upload_target}}
      uses: actions/upload-artifact@v3
      with:
        name: ${{matrix.config.artifact_name}}
        path: src/${{matrix.config.target}}
